class HttpConfig {
  static const String baseUrl = '';
  static const Duration timeout = Duration(
      days: 0,
      hours: 0,
      minutes: 0,
      seconds: 5,
      milliseconds: 000,
      microseconds: 00);
}
